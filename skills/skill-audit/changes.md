# Changes ledger

Changes prior audits have driven. The audit reads this every run so it does not re-flag something already fixed, and so it can say when a fixed problem recurred anyway. Add a row by hand after you make a change; the skill never writes here.

| Date | Change | Action class | Where the diff is recorded |
| --- | --- | --- | --- |
